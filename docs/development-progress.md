# 开发进展记录（单功能推进范式）

> 范式说明：每次只推进一个功能块，记录分为两段：
>
> 1) 已完成任务（仅记录已落地且可验证项）
> 2) 下一步任务（以模块的形式呈现）

---

## 已完成任务

- 数据库表结构与约束初始化
- 前后端认证主链路与权限路由拦截
- 基础系统框架搭建（前后端联调测试通过）
- **管理员用户管理模块（完整落库版）**
  - 后端采用标准 3 层架构重构（Router -> Service -> Schema/Model）
  - 实现用户列表分页、模糊搜索过滤
  - 支持新增与编辑用户，强制校验邮箱格式与角色仓库绑定规则
  - 实现基于角色的状态修改与管理员不可禁用的防呆拦截
- **客户与产品管理模块（完整落库版）**
  - 客户/产品后端 CRUD、分页搜索、批量软删除、状态启停接口
  - 产品图片上传、替换、移除全链路完成（数据库 URL + 资源文件同步）
  - 后端静态资源挂载与前端访问打通（/resources）
  - 前端客户与产品页面落地，支持状态开关（软删除可见）

---

## TODO
**增强仓库管理模块（与库存管理联动（暂时不实现流水））**
  - 可以在仓库管理的列表中点击仓库名字进入该仓库的库存详情页（新的页面，不是原地跳转），库存详情页展示该仓库内所有商品库存的列表。（对于下架商品（is_active = false）,把它展示为不可用状态）。
  - 支持搜索过滤库存项（按产品名称、SKU、类别等）
  - 支持人工盘点修正库存数量（由于还没暂时不用考虑异常状态）
  - 仓库的库存详情页面的最上方得展示出仓库的名字、地址和一个很宽的图片（类似门户网站的设计）（如果有的话）。如果没有图片，就展示一个默认的仓库图片。
  
  
